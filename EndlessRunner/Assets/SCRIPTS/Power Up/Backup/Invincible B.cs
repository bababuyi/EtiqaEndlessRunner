using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Invincible : MonoBehaviour
{
    /*
    public float invincibilityDuration = 5f; // How long the player is invincible

    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("Player")) // Make sure the player has the tag "Player"
        {
            Movement player = other.GetComponent<Movement>();
            if (player != null)
            {
                player.ActivateInvincibility(invincibilityDuration);
            }

            Destroy(gameObject); // Remove power-up after collection
        }
    }
    */
}
